Fixture notes for analysis parity:

- Milestone 3 keeps the analysis cases inline in `tests/parity_analysis.rs` while the report and block models stabilize.
- The coverage here focuses on five-hour block rollover, gap insertion, limit attachment, custom-plan fallback, and shared daily totals.
- Static analysis fixtures can expand later once upstream oracle snapshots are wired into the report pipeline.

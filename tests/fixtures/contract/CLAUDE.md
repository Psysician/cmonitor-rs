# CLAUDE.md

## Overview

The `tests/fixtures/contract` directory documents how CLI and config parity inputs are sanitized and refreshed.

## Index

| File | Contents (WHAT) | Read When (WHEN) |
| --- | --- | --- |
| `CLAUDE.md` | Contract-fixture directory index and task map | Locating fixture refresh guidance for CLI parity |
| `README.md` | Contract fixture sanitization and oracle refresh instructions | Regenerating CLI/config parity inputs |
